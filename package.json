{
  "name": "root",
  "version": "1.2.3",
  "description": "NPM Monorepo Package to test versioning strategies and dist-tags and see how `lerna publish` and `npm install my-package --tag whatever` may differ from `npm install my-package@whatever` ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jon Rogers <jon@rcode5.com>",
  "license": "WTFPL",
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
